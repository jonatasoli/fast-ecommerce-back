
from app.infra.constants import MediaType

def test_image_in_gallery_must_upload():

    # Assert
    assert media.order == 1
    assert media.type == MediaType.photo
    assert media.uri == image_path
